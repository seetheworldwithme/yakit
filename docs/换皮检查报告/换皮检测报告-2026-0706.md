# 前端同源换皮检测报告

> 生成日期：2026-07-06
> 评分模型：独立性得分 v2（满分 100，越高越独立）
> 工具：`.agents/skills/detecting-frontend-reskins`（`scripts/reskin_audit.py`）
> 对比对象：
> - **A = yakit-update-ui(Sentinel)**（`/Volumes/coding/application/yakit-update-ui`）
> - **B = yakit(上游)**（`/Volumes/coding/application/yakit`）

---

## 一、结论

- **独立性得分：36.9 / 100**　❌ **高度疑似换皮**
- 竞标安全线（≥70）：❌ 未达标，差 33.1 分
- 同源身份铁证：package name + 主版本相同：`yakit@1`

## 二、评分模型

**总分 100 = 独立性得分**（越高越独立，100 = 完全独立，0 = 完全相同）。每维度独立分 = 满分 ×（1 − 重叠度）；检测到同源重叠即扣分。

| 维度 | 满分 | 检测方向 |
| --- | ---: | --- |
| 技术骨架 | 25 | 目录结构、技术栈选型、工程配置、核心工具链复用 |
| 页面布局 | 30 | 路由结构、组件层级、页面元素排版逻辑 |
| 视觉体系 | 25 | 色彩系统、字体规范、图标库、通用组件样式 |
| 业务逻辑 | 20 | 核心业务流程、状态管理逻辑、接口调用逻辑 |

| 独立性得分 | 等级 | 含义 |
| ---: | --- | --- |
| **≥70** | ✅ 完全独立项目 | **竞标安全区（迭代目标）**，常规代码审计也判为独立 |
| 40–69 | ⚠️ 存在部分复用痕迹 | 有同源线索，需继续迭代高危维度 |
| **≤39** | ❌ 高度疑似换皮 | 同源可识别，**禁止竞标** |

> **身份 override**：git origin 相同，或 package name + 主版本相同 → 总分钳制 ≤39（竞标方一眼可见，无法用代码改造解释）。

## 三、维度得分

| 维度 | 满分 | 重叠度 | 独立分 | 扣分 |
| --- | ---: | ---: | ---: | ---: |
| 技术骨架 | 25 | 96.2% | 0.95 | 24.05 |
| 页面布局 | 30 | 65.3% | 10.41 | 19.59 |
| 视觉体系 | 25 | 47.8% | 13.04 | 11.96 |
| 业务逻辑 | 20 | 37.5% | 12.5 | 7.5 |
| **合计** | **100** | — | **36.9** | **63.1** |

## 四、子指标扣分明细

### 技术骨架（满分 25，独立分 0.95，扣分 24.05）

| 子指标 | 份额 | 重叠度 | 子指标总分 | 扣分 |
| --- | ---: | ---: | ---: | ---: |
| 依赖版本重叠 | 20 | 100.0% | 5.0 | 5.0 |
| 框架/构建重叠 | 15 | 100.0% | 3.75 | 3.75 |
| 目录结构重叠 | 20 | 97.9% | 5.0 | 4.89 |
| 路由/配置重叠 | 20 | 83.3% | 5.0 | 4.17 |
| 状态管理重叠 | 15 | 100.0% | 3.75 | 3.75 |
| 工程配置相似度 | 10 | 99.7% | 2.5 | 2.49 |

**关键证据：**

- 同版本依赖（47 项，抽样）：`@electron/notarize`(^2.5.0↔^2.5.0), `@grpc/grpc-js`(^1.3.6↔^1.3.6), `@grpc/proto-loader`(^0.6.4↔^0.6.4), `@testing-library/jest-dom`(^5.16.5↔^5.16.5), `adm-zip`(^0.5.16↔^0.5.16), `axios`(0.26.1↔0.26.1), `chalk`(5.6.2↔5.6.2), `chrome-launcher`(^0.15.0↔^0.15.0), `commander`(13.1.0↔13.1.0), `compressing`(^1.8.0↔^1.8.0), `concurrently`(^6.2.0↔^6.2.0), `cross-spawn`(^7.0.6↔^7.0.6)
- 共同框架：`vite`
- 共同状态库：`zustand`
- 共同目录签名（抽样）：`.github`, `.github/ISSUE_TEMPLATE`, `.github/ISSUE_TEMPLATE/*.yml`, `.github/actions`, `.github/actions/pr-ci-setup-node-yarn`, `.github/actions/pr-ci-setup-node-yarn/*.yml`, `.github/actions/pr-ci-yarn-install`, `.github/actions/pr-ci-yarn-install/*.yml`, `.github/actions/sign-windows`, `.github/actions/sign-windows/*.yml`, `.github/workflows`, `.github/workflows/*.yml`, `.vscode`, `.vscode/*.json`, `app`
- 共同路由（抽样）：`/login`, `/wp-content/themes/x/style.css`

### 页面布局（满分 30，独立分 10.41，扣分 19.59）

| 子指标 | 份额 | 重叠度 | 子指标总分 | 扣分 |
| --- | ---: | ---: | ---: | ---: |
| 组件层级/标签序列重叠 | 30 | 100.0% | 9.0 | 9.0 |
| 页面分区/class 命名重叠 | 20 | 17.7% | 6.0 | 1.06 |
| 共享组件实现重叠 | 25 | 84.6% | 7.5 | 6.35 |
| 响应式/断点逻辑重叠 | 15 | 6.5% | 4.5 | 0.29 |
| 布局工具模式重叠 | 10 | 96.2% | 3.0 | 2.89 |

**关键证据：**

- 组件层级最佳匹配（抽样）：
  - `app/renderer/electron/electron.html` ↔ `app/renderer/electron/electron.html`（重叠 100.0%）
  - `app/renderer/src/main/public/index.html` ↔ `app/renderer/src/main/public/index.html`（重叠 100.0%）
  - `app/renderer/src/main/public/yakit-aux.html` ↔ `app/renderer/src/main/public/yakit-aux.html`（重叠 100.0%）
  - `app/renderer/src/main/src/index.tsx` ↔ `app/renderer/src/main/src/index.tsx`（重叠 100.0%）
  - `app/renderer/src/main/src/NewApp.tsx` ↔ `app/renderer/src/main/src/NewApp.tsx`（重叠 100.0%）
  - `app/renderer/src/main/src/ChildNewApp.tsx` ↔ `app/renderer/src/main/src/ChildNewApp.tsx`（重叠 100.0%）
- 共同 class/分区名（抽样）：`__floater__arrow`, `__floater__body`, `absolute`, `absolute-box`, `accent-content`, `acceptmetacard`, `acceptmetapre`, `acceptmetatitle`, `accountadminpage`, `accountlist`, `accountlist-table-extra`, `action-btn-group`, `action-buttons`, `action-icon`, `action-icon-ban-disabled`
- 共享组件实现指纹碰撞（实现几乎相同）：
  - `app/renderer/src/main/src/auxWindow/components/AuxXterm/useAuxTerminalPush.ts` ↔ `app/renderer/src/main/src/auxWindow/components/AuxXterm/useAuxTerminalPush.ts`（fp `11cdf415ea5d`）
  - `app/renderer/src/main/src/auxWindow/components/AuxXterm/AuxXterm.tsx` ↔ `app/renderer/src/main/src/auxWindow/components/AuxXterm/AuxXterm.tsx`（fp `b6e29a3b1639`）
  - `app/renderer/src/main/src/auxWindow/components/AuxXterm/index.ts` ↔ `app/renderer/src/main/src/auxWindow/components/AuxXterm/index.ts`（fp `55a5dbafac3e`）
  - `app/renderer/src/main/src/auxWindow/components/AuxXterm/AuxXterm.module.scss` ↔ `app/renderer/src/main/src/auxWindow/components/AuxXterm/AuxXterm.module.scss`（fp `89778158fe28`）
  - `app/renderer/src/main/src/auxWindow/components/AuxXterm/xtermTheme.ts` ↔ `app/renderer/src/main/src/auxWindow/components/AuxXterm/xtermTheme.ts`（fp `3c10801a07d6`）
  - `app/renderer/src/main/src/auxWindow/components/ConcurrentStreamSkeleton/ConcurrentStreamSkeleton.module.scss` ↔ `app/renderer/src/main/src/auxWindow/components/ConcurrentStreamSkeleton/ConcurrentStreamSkeleton.module.scss`（fp `aa087cde8831`）
  - `app/renderer/src/main/src/auxWindow/components/ConcurrentStreamSkeleton/ConcurrentStreamSkeleton.tsx` ↔ `app/renderer/src/main/src/auxWindow/components/ConcurrentStreamSkeleton/ConcurrentStreamSkeleton.tsx`（fp `057918d220e8`）
  - `app/renderer/src/main/src/components/ResizeLine.tsx` ↔ `app/renderer/src/main/src/components/ResizeLine.tsx`（fp `5960e28373b2`）
- 共同断点/响应式模式：`matchmedia`, `sm:`

### 视觉体系（满分 25，独立分 13.04，扣分 11.96）

| 子指标 | 份额 | 重叠度 | 子指标总分 | 扣分 |
| --- | ---: | ---: | ---: | ---: |
| 配色系统重叠 | 30 | 56.0% | 7.5 | 4.2 |
| 字体比例重叠 | 25 | 52.2% | 6.25 | 3.27 |
| 间距比例重叠 | 20 | 40.5% | 5.0 | 2.03 |
| 动效规则重叠 | 15 | 13.8% | 3.75 | 0.52 |
| 设计 token 命名重叠 | 10 | 78.3% | 2.5 | 1.96 |

**关键证据：**

- 共同颜色值（抽样）：`#000`, `#0000`, `#000000`, `#0000004d`, `#000c`, `#001587`, `#0065a9`, `#007acc`, `#0080fc`, `#009fe8`, `#00a2b9`, `#00c090`, `#00cd00`, `#00d26a`, `#00d6b9`
- 共同 CSS 变量/token 名（抽样）：`--angle`, `--antd-wave-shadow-color`, `--arrow-top`, `--auto-complete-background-color`, `--auto-complete-font-color`, `--bg-color`, `--bg-hover-color`
- 共同字体规则：`font-family:'courier new', courier, monospace`, `font-family:'courier new, courier, monospace'`, `font-family:'monaco', 'menlo', 'ubuntu mono', monospace`, `font-family:'monospace'`, `font-family:'pingfang hk'`, `font-family:'pingfang sc'`, `font-family:-apple-system,`, `font-family:-apple-system, blinkmacsystemfont, "segoe ui", "roboto", "oxygen", "ubuntu", "cantarell", "fira sans", "droid sans", "helvetica neue", sans-serif`, `font-family:-apple-system, blinkmacsystemfont, "segoe ui", roboto, "helvetica neue", arial, "noto sans", sans-serif, "apple color emoji", "segoe ui emoji", "segoe ui symbol", "noto color emoji"`, `font-family:-apple-system, blinkmacsystemfont, 'segoe ui', 'roboto', 'oxygen', 'ubuntu', 'cantarell', 'fira sans', 'droid sans',`
- 共同间距规则：`"0 12px"`, `"0 16px"`, `"0 4px"`, `"3px 4px"`, `"5px 0px 0px 5px", top: 1`, `'0 0 0 16px'`, `'0 0px 4px 24px', width: '100%', margintop: 8`, `'0 10px'`, `'0 12px'`, `'0 15px 15px',`
- 共同动效规则：`@keyframes:ai-chat-item-highlight`, `@keyframes:ai-loading-char-fade-in`, `@keyframes:ai-loading-cursor-blink`, `@keyframes:blink`, `@keyframes:building-blocks`, `@keyframes:changebgcolor`, `@keyframes:dotelastic`, `@keyframes:dotelasticafter`, `@keyframes:dotelasticbefore`, `@keyframes:fadein`

### 业务逻辑（满分 20，独立分 12.5，扣分 7.5）

| 子指标 | 份额 | 重叠度 | 子指标总分 | 扣分 |
| --- | ---: | ---: | ---: | ---: |
| 业务逻辑 token 重叠 | 35 | 48.9% | 7.0 | 3.43 |
| 第三方服务配置重叠 | 25 | 23.6% | 5.0 | 1.18 |
| API 端点/集成重叠 | 20 | 38.7% | 4.0 | 1.55 |
| 工具函数指纹重叠 | 20 | 33.7% | 4.0 | 1.35 |

**关键证据：**

- 共同第三方服务：`axios`, `fetch`, `firebase`, `graphql`, `gtag`, `sentry`
- 共同 API 端点（抽样）：`//`, `///`, `/__test__/`, `/__tests__/`, `/admin`, `/admin/`, `/api`, `/api/auth/challenge`, `/api/auth/login`, `/api/data/delete`, `/api/get-challenge`, `/api/login`, `/api/offline-mock`, `/api/order/create`, `/api/pipeline/bootstrap`
- 共同函数名（抽样）：`$e`, `$f`, `$i`, `$l`, `$m`, `$n`, `$p`, `$pos`, `$s`, `$u`, `$y`, `__platform__`, `_client`, `_d`, `_data`
- 高频共享逻辑 token：`const`, `return`, `div`, `if`, `0`, `from`, `import`, `t`, `e`, `string`, `styles`, `item`, `classname`, `data`, `32`, `path`, `export`, `value`, `false`, `type`

## 五、同源身份信号

| 项 | A | B | 命中 |
| --- | --- | --- | --- |
| git origin | `https://github.com/seetheworldwithme/yakit-update-ui.git` | `https://github.com/yaklang/yakit.git` | — |
| package name | `yakit` | `yakit` | ✅ |
| package version | `1.4.7-0626` | `1.4.7-0630` | 主版本同 |

**身份 override 生效**：package name + 主版本相同：`yakit@1` → 总分钳制 ≤39。这是竞标场景下最致命的识别信号，必须首先改造（改包名 / 切换 git 远端 / 拆分仓库）。

## 六、高危信号

- ⚠️ 技术骨架重叠 96.2% > 70%，目录/依赖/构建几乎完全复用。
- ⚠️ 页面组件层级重叠 100.0% > 65%，页面组装方式未重建。

## 七、优化建议清单（按可回收分降序）

> 「可回收分」= 把该子指标重叠压到 15%（独立档残留）后能恢复的独立性分数。优先做回收分高的项，性价比最高。

| 优先级 | 维度 | 子指标 | 当前重叠 | 已扣分 | 预期回收 | 动作 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| P0 | 页面布局 | 组件层级/标签序列重叠 | 100.0% | 9.0 | **+7.65** | 重排主框架/首页布局结构（双栏↔栅格↔Tab↔卡片墙），重命名容器组件，拆/合页面，打散 JSX 标签序列。 |
| P0 | 页面布局 | 共享组件实现重叠 | 84.6% | 6.35 | **+5.22** | 重写共享组件实现细节（props 形态、内部结构、默认值、文件位置），消除组件指纹碰撞。 |
| P1 | 技术骨架 | 依赖版本重叠 | 100.0% | 5.0 | **+4.25** | 核心依赖升大版本或替换等价库（axios→ky/fetch、zustand→jotai、antd→@mui/自研、moment↔dayjs），打散依赖版本指纹。 |
| P1 | 技术骨架 | 目录结构重叠 | 97.9% | 4.89 | **+4.14** | 重组顶层目录命名与层级（pages→views、components→ui、store→state、utils→lib），改变目录签名。 |
| P1 | 技术骨架 | 路由/配置重叠 | 83.3% | 4.17 | **+3.42** | 重构路由表组织（集中式↔约定式、改 path 命名风格、拆分路由模块），打散路由指纹。 |
| P1 | 技术骨架 | 框架/构建重叠 | 100.0% | 3.75 | **+3.19** | 调整构建/框架选型或构建配置（CRA↔Vite↔Next、webpack↔vite、tsconfig 严格度），改 build 工具链指纹。 |
| P1 | 技术骨架 | 状态管理重叠 | 100.0% | 3.75 | **+3.19** | 替换状态管理库或重构 store 切片边界与命名（zustand↔jotai↔redux-toolkit）。 |
| P1 | 视觉体系 | 配色系统重叠 | 56.0% | 4.2 | **+3.07** | 更换主色相并重建色板，迁移到独立 token 命名空间（旧名仅作兼容别名）。 |
| P2 | 页面布局 | 布局工具模式重叠 | 96.2% | 2.89 | **+2.44** | 替换/重命名布局工具函数（间距/栅格/flex 辅助），改布局工具指纹。 |
| P2 | 业务逻辑 | 业务逻辑 token 重叠 | 48.9% | 3.43 | **+2.38** | 重构业务模块命名与控制流（函数拆分、改写算法实现、调整调用层次），打散逻辑 token 指纹。 |
| P2 | 视觉体系 | 字体比例重叠 | 52.2% | 3.27 | **+2.33** | 重建字体比例与字族选型（modular scale↔固定档、换字体族），改字体指纹。 |
| P2 | 技术骨架 | 工程配置相似度 | 99.7% | 2.49 | **+2.12** | 替换 lint/format/prettier/eslint 规则集与 npm scripts 命名、husky/commitlint 配置。 |
| P2 | 视觉体系 | 设计 token 命名重叠 | 78.3% | 1.96 | **+1.58** | 全量重命名 CSS 变量/设计 token 到独立命名空间（如 --App-*→自有前缀）。 |
| P3 | 视觉体系 | 间距比例重叠 | 40.5% | 2.03 | **+1.28** | 重建间距比例（8pt↔4pt↔baseline grid），改间距指纹。 |
| P3 | 业务逻辑 | API 端点/集成重叠 | 38.7% | 1.55 | **+0.95** | 重构请求层封装与 URL 命名风格（REST↔RPC、path 命名、query 风格）。 |
| P3 | 业务逻辑 | 工具函数指纹重叠 | 33.7% | 1.35 | **+0.75** | 替换/提取工具集，改函数签名与命名风格。 |

## 八、迭代路径（目标 ≥70)

- 当前独立性：**36.9 / 100**，距安全线 ≥70 还差 **33.1 分**。
- 最大可回收：**47.96 分**（将所有 >15% 的子指标重叠压至 15%）。
- 可行性：✅ 最大可回收 ≥ 差值，达到安全线**可行**。

**推荐先做 Top 5 改动**（累计可回收 **24.68 分**）：

1. [P0] 页面布局 / 组件层级/标签序列重叠（当前 100.0%，可回收 +7.65）—— 重排主框架/首页布局结构（双栏↔栅格↔Tab↔卡片墙），重命名容器组件，拆/合页面，打散 JSX 标签序列。
2. [P0] 页面布局 / 共享组件实现重叠（当前 84.6%，可回收 +5.22）—— 重写共享组件实现细节（props 形态、内部结构、默认值、文件位置），消除组件指纹碰撞。
3. [P1] 技术骨架 / 依赖版本重叠（当前 100.0%，可回收 +4.25）—— 核心依赖升大版本或替换等价库（axios→ky/fetch、zustand→jotai、antd→@mui/自研、moment↔dayjs），打散依赖版本指纹。
4. [P1] 技术骨架 / 目录结构重叠（当前 97.9%，可回收 +4.14）—— 重组顶层目录命名与层级（pages→views、components→ui、store→state、utils→lib），改变目录签名。
5. [P1] 技术骨架 / 路由/配置重叠（当前 83.3%，可回收 +3.42）—— 重构路由表组织（集中式↔约定式、改 path 命名风格、拆分路由模块），打散路由指纹。

## 九、方法说明与局限

- 评分基于源码树**静态分析**（依赖、目录、路由、JSX 标签序列、CSS 值/token、函数名/端点字符串等），**不含运行时行为对比**。
- 已自动忽略 `node_modules` / `dist` / `build` / `.git` 等生成物；lockfile 噪声未计入依赖身份判断。
- 同框架（CRA / Vite / Next / antd 等）会产生一部分「合理重叠」，但身份 override 与多维高分交叉验证可区分真同源与框架巧合。
- 独立性得分是**同源可识别度的工程指示器**，迭代目标是 ≥70（完全独立项目 / 竞标安全区）。

---

## 附录：原始产物与复跑命令

```bash
python3 .agents/skills/detecting-frontend-reskins/scripts/reskin_audit.py \
  /Volumes/coding/application/yakit-update-ui /Volumes/coding/application/yakit \
  --output /tmp/reskin-audit --format both --lang zh
```