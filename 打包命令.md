## 打 tools（先打，Memfit 会引用它）
```
cd /Volumes/code/JZ_Memfit/tools

# 打包前端
npm run build

# 使用 docker 启动前端（nginx 监听 18888）
docker-compose up -d

# 打包后端
GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -C backend -o bin/server ./cmd/server

# 启动后端（监听 18080，nginx 会反向代理到这个端口）
cd /tools/backend/bin&./server
```



## 打 Memfit（再打）
> Memfit 打包后默认使用 web 模式，通过 `http://localhost:18888` 连接 Docker 中运行的 tools。
> 使用前需确保 tools 已通过上方命令启动（docker-compose + Go 后端）。

```
cd /Volumes/code/JZ_Memfit/yakit

# 安装依赖
yarn install

# 构建 memfit 渲染层
yarn build-renders-memfit

# 生成 mac arm64 dmg
npx env-cmd -e nonSignLegacy,Memfit -r packageScript/.env-cmdrc \
  electron-builder build --mac dmg --arm64 --config ./packageScript/electron-builder.config.js
# 产物：yakit/release/MemfitAI-*-darwin-legacy-arm64.dmg

# 生成 Windows x86 exe
npx env-cmd -e nonSignLegacy,Memfit -r packageScript/.env-cmdrc \
  electron-builder build --win nsis --ia32 --config ./packageScript/electron-builder.config.js
# 产物：yakit/release/MemfitAI-*-windows-legacy-x86.exe
```